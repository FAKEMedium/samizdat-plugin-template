// Route-specific JS for /skeleton. Rendered into $web->{script} and wrapped in a
// DOMContentLoaded handler by the bootstrap layout — do NOT add your own listener.
// fetch('/skeleton', {headers: {Accept: 'application/json'}}).then(...);
