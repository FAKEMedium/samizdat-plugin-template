-- Fresh-snapshot baseline for the `skeleton` schema. Author later schema changes as
-- new version dirs: 40-skeleton/2/up.sql + down.sql (pgModeler diff dumps drop in).
CREATE SCHEMA IF NOT EXISTS skeleton;

-- CREATE TABLE skeleton.items (
--   id     SERIAL PRIMARY KEY,
--   name   TEXT NOT NULL
-- );
